# 插件开发规范

## 当前优化范围：端点吸附与选区轮询

本轮仅允许修改以下两项：

1. SketchUp 端路径端点自动吸附的性能与分组正确性。
2. Illustrator 面板选区轮询，以及导出完成状态保持到下一次面板操作。

PNG 文件清理、临时文档恢复、JSON 写入、导入结果反馈、图片材质刷新和白色材质处理不在本轮范围内。除上述两项外，不调整面板布局、导入导出功能或数据协议。

每项改动必须保留对应自动化检查；SketchUp 端无法在命令行运行时，须在 SketchUp 热重载后完成实机验证。
